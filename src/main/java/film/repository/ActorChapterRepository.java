package film.repository;

import film.models.ActorChapter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ActorChapterRepository extends JpaRepository<ActorChapter, Long> {
}